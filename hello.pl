use strict;
use warnings;

# my $filename = "demo2.txt";
# open(FH, $filename); # open file
# my @lines = <FH>; # read
# close(FH);
# my $count = scalar @lines; 
# print("There are $count lines in $filename\n");


main(@ARGV);

sub main {
  print "Hello World\n";
  my @arr = ( 1..10);
}